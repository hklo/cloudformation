[{
  "cpu": 256,
  "essential": true,
  "image": "${image_url}",
  "memory": 256,
  "name": "${container_name}",
  "portMappings": [{
    "containerPort": 8080,
    "hostPort": 0
  }],
  "mountPoints": [{
    "containerPath": "/logs",
    "sourceVolume": "logs"
  }]
}]